const CharacterItem = ({item}) => {
    return (
        <img src={item}
             alt="characters Rick and Morty"
             width={200}
             height={200}
        />
    );
};

export default CharacterItem;